export enum ButtonMode {
    yellow = 'yellow',
    green = 'green',
    outlined = 'outlined'
}

export enum InputMode {
    green = 'green',
    error =  'error',
    yellow = 'yellow'
}

export enum FieldType {
    textarea = 'textarea',
    input = 'input'
}
